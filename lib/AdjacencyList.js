// Represents an Adjacency LIst
function AdjacencyList(head) {
    // Keep track of the head node of the list
    this.head = head;
}

let Immutable = require('immutable');
let List = Immutable.List;

// List API
let list = new List()