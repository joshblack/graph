const assert = require('assert');
const Graph = require('../lib/Graph');

describe('Graph', function () {
    // It should return all vertices if the vertices option is passed in
    // let g = Graph({ vertices: ['a', 'b', 'c', 'd'] });

    // It should return [a, b, c, d] as all the vertices
    // let g = Graph({ directed: [['a', 'b'], ['b', 'c'], ['c', 'd']] });

    // It should return [a, b, c, d] as all the vertices
    // let g = Graph({ undirected: [['a', 'b', 'c'], ['c', 'd']] });

    // It should return [a, b, c, d] as all the vertices
    // let g = Graph({ weighted: [['a', 'b', 1], ['b', 'c', 1], ['c', 'd', 1]] });
});